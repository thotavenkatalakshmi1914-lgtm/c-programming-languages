/*program for to check age is eligible or not*/
#include<stdio.h>
int main(){
	int age;
	printf("enter your age:");
	scanf("%d",&age);
	if(age)
	printf("you are eligible for vote");
	else
	printf("you are not eligible for vote");
	return 0;
}
